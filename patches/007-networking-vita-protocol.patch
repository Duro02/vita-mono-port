diff --git i/mono/utils/networking-posix.c w/mono/utils/networking-posix.c
index e307a728..99c9c66d 100644
--- i/mono/utils/networking-posix.c
+++ w/mono/utils/networking-posix.c
@@ -174,6 +174,17 @@ fetch_protocol (const char *proto_name, int *cache, int *proto, int default_val)
 	return *proto;
 }
 
+#elif defined(__vita__)
+/* Vita: newlib 无 getprotobyname, 直接用默认协议号 */
+static int
+fetch_protocol (const char *proto_name, int *cache, int *proto, int default_val)
+{
+	(void)proto_name;
+	*proto = default_val;
+	*cache = 1;
+	return *proto;
+}
+
 #endif
 
 int
